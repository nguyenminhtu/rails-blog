<%= render "layouts/navigation_admin" %>

<div class="container">
	<div class="row">
		<%= render "layouts/sidebar_admin" %>

		<div class="col-md-10 text-center">
			<h1>Hello Admin</h1>
			<hr>
			<h4>Now we have <%= pluralize(Post.count, "post") %> in database.</h4>
			<h4>Now we have <%= pluralize(Category.count, "category") %> in database.</h4>
			<h4>Now we have <%= pluralize(User.count, "user") %> in database.</h4>
			<br>
			<br>
			<br>
			<br>
			<br>
			<br>
			<br>
			<br>
			<br>
			<br>
			<br>
			<br>
			<br>
			<br>
			<br>
			<br>
			<br>
		</div>
	</div>
</div>

<%= render 'layouts/footer' %>